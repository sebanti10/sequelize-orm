/*const { getPlaylists,
        addNewPlaylist,
        getPlaylistWithID,
        updatePlaylist,
        deletePlaylist} = require('./../controllers/ormController');

const routes = (app) => {
    app.route('/api/playlists')

        //get all contacts
        .get(getPlaylists)

        //Post endpoint
        .post(addNewPlaylist);

    app.route('/api/playlists/:id')
        //get  a specific playlist
        .get(getPlaylistWithID)

        //update a specific playlist
        .put(updatePlaylist)

        //delete a specific playlist
        .delete(deletePlaylist);
}

module.exports = routes;
*/